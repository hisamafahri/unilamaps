import React, { ReactElement } from 'react'
import { Text } from 'react-native'
import Base from '../../component/container/Base'

export default function Location({ }: any): ReactElement {
    return (
        <Base>
            <Text>Location</Text>
        </Base>
    )
}
