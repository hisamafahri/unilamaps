import React, { ReactElement } from 'react'
import { Text } from 'react-native'
import Base from '../../component/container/Base'

export default function Search({ }: any): ReactElement {
    return (
        <Base>
            <Text>Search</Text>
        </Base>
    )
}
